{
  "name": "rg-pages-builder",
  "private": true,
  "type": "module",
  "description": "Out-of-tree build for the admin Pages visual editor (Puck). Produces the committed /assets/pages-builder.bundle.{js,css}. NOT part of the Vercel deploy (excluded via /.vercelignore). Build recipe: npm install && npm run build.",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@puckeditor/core": "0.22.2",
    "esbuild": "^0.28.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
