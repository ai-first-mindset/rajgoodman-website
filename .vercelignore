# Out-of-tree admin build toolchain — never part of the Vercel deploy.
# The built artifact is committed to /assets/pages-builder.bundle.{js,css}.
# Keeping this dir (and node_modules) out ensures Vercel stays zero-config static
# (no root package.json → no build step).
tools/
node_modules/
