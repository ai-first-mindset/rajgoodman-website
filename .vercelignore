# The site stays zero-config static: no root package.json, no build step.
#
# NOTE: /builder is deliberately NOT ignored. It is plain ESM with no
# dependencies, served as-is to the admin and imported directly by the
# serverless page renderer — the same modules on both sides, no build artifact.
node_modules/
